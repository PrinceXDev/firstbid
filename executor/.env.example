# Firstbid — Shannon testnet ONLY. Throwaway key. Never fund with real value.
FIRSTBID_PRIVATE_KEY=0x_your_throwaway_testnet_private_key
FIRSTBID_ADDRESS=0x_derived_address
FIRSTBID_INDEXER_URL=https://dev.smk.somnia.host/v1/graphql
FIRSTBID_WS_RPC_URL=wss://api.infra.testnet.somnia.network/ws
FIRSTBID_PORT=8787
# Required before the reference executor will accept signer-backed routes.
# Generate one with: openssl rand -hex 32
FIRSTBID_EXECUTOR_TOKEN=
