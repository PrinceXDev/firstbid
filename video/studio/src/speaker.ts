/** The presenter card in the closing act.
 *
 *  Drop a portrait at `video/studio/public/speaker.jpg` (or .png and change
 *  `file` below) and flip `enabled` to true. Until then the outro renders a
 *  designed monogram in the same frame, so the film always builds. */
export const SPEAKER = {
  enabled: false,
  file: "speaker.jpg",
  name: "Prince Panchani",
  role: "Firstbid · Somnia × DreamDEX",
  handle: "github.com/PrinceXDev/firstbid",
  initials: "PP",
} as const;
